import React from "react";
import ReactDOM from "react-dom";

const App = () =>{
    return (
        <div>
            This is a new fresh react app.
        </div>
    );
}

ReactDOM.render(<App />, document.querySelector('#root'));